version: '3.8'

services:
  gmail-notifier:
    build: .
    container_name: gmail-notifier
    ports:
      - "3000:80"
    volumes:
      - ./config.json:/app/config.json
      - ./logs:/app/logs
    environment:
      - TZ=Asia/Bangkok
    restart: unless-stopped
